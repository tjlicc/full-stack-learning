{
  "name": "phonebook-backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf build && cd ../../part2/exercise-phonebook && npm run build && cp -r build ../../part3/exercise-phonebook-backend",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "./node_modules/.bin/nodemon index.js",
    "start": "node index.js",
    "lint": "eslint ."
  },
  "author": "licc",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "eslint": "^7.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.18",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
